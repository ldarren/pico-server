exports.setup = function(){
  this.
}

exports.born = function(obj){
  var
  oldProtos = func.prototype,
  newProtos = new base;

  newProtos.parent = base.prototype;
  newProtos.constructor = child;
  for (key in oldProtos) newProtos[key] = oldProtos[key];

  func.prototype = newProtos;
  pico.modules[name] = func;
}
